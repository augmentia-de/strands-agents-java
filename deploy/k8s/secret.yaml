apiVersion: v1
kind: Secret
metadata:
  name: strands-agent-secret
  labels:
    app: strands-agent
type: Opaque
stringData:
  OPENAI_API_KEY: "your-api-key-here"
  # VAULT_TOKEN: "your-vault-token" # optional
