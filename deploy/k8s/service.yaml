apiVersion: v1
kind: Service
metadata:
  name: strands-agent
  labels:
    app: strands-agent
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: http
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    app: strands-agent
